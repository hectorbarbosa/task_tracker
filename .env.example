# Server
APP_PORT=8080
APP_ENV=development

# MySQL (local dev — points to docker-compose.dev.yml services)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=task_user
DB_PASS=task_pass
DB_NAME=task_tracker

# Redis (local dev)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# JWT
JWT_SECRET=dev-secret-change-me
JWT_EXPIRATION_HOURS=24
